{
  "name": "sample-heroku-app",
  "version": "1.0.0",
  "description": "A sample app deployable on heroku",
  "main": "server.js",
  "engines": {
    "node": "14.x"
  },
  "scripts": {
    "build": "rm -rf dist/ && node_modules/.bin/tsc && node_modules/.bin/copyfiles webroot/** dist/",
    "start": "node dist/server.js"
  },
  "author": "Jay",
  "license": "WTFPL",
  "dependencies": {
    "copyfiles": "^2.4.1",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.11",
    "typescript": "^4.1.5"
  }
}
